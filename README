REST-PROJECT:


rest-client:

AngularJs
auth: Basic (not secure)
almost no validation
main aim of the project was to expose REST endpoints and use them

____________________________

eve-python-rest:

simple REST server working with MongoDB

____________________________

mongodb:	sudo service mongod stop; mongod --dbpath=/home/filipmqv/tiwpr/rest-eve-python/data/db/
gui:		robomongo 		// from proper dir
server:		python api.py
angular:	grunt serve		// just for development
db manager:	mongo			// console mongoDB manager 


uruchomienie w sieci wewnętrznej:
api.py: (server)
	#app.run(threaded=True)
	app.run(threaded=True,host="0.0.0.0",port=int("5000"))
services.js: (client)
	//var lhost = 'http://localhost:5000/';
	var lhost = 'http://192.168.1.4:5000/';
Gruntfile.js: (client)
	//hostname: 'localhost',
	hostname: '0.0.0.0',



############################
############################
############################

WEBSOCKETS-PROJECT:

uruchom serwer:		npm start














